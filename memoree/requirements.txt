# Memoree v3 — Python 3.11+

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
aiosqlite>=0.20.0
pyyaml>=6.0.1
httpx>=0.27.0

# Vector store (primary)
lancedb>=0.10.0

# Vector store (fallback)
qdrant-client>=1.10.0

# Embeddings (CPU-only, no GPU needed)
sentence-transformers>=3.1.0

# LLM SDKs
google-generativeai>=0.8.0           # Gemini
# openai>=1.40.0                      # uncomment for SuperGrok (OpenAI-compat)

# Sync
# powersync-py>=0.4.0                 # pip install powersync when stable

# Optional: MemOS bridge
# requests>=2.32.0                    # already pulled by httpx transitive

# Dev
python-dotenv>=1.0.0
