import VAPCardGenerator from './VAPCardGen';  // ← just ./ now, since it's in src/

function App() {
  return (
    <div className="min-h-screen bg-black">
      <VAPCardGenerator />
    </div>
  );
}

export default App;