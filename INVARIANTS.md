---
type: standard-framework-foundational
title: ecosys — Organism Invariants
description: Organism locks for the Type-1 Civilization stack. Lattice, contractions, naming, shared geometry, VIM β-flow, and SAGES G_13. Does not reprint volume papers.
workspaces: rossaedwards/ecosys, aurphyx/ecosys
services:
  - Aura
  - Audry
  - AuraFS
  - Fuxyez
  - g0dm0d3
  - Memoree
  - SAGES
  - VASP
domains:
  - systems
  - cognition
  - audio
  - identity
  - xessability
  - governance
nodes:
  - SIX⊗SIX
  - SIX⊗SCX
  - SIX⊗ICX
  - SCX⊗SIX
  - SCX⊗SCX
  - SCX⊗ICX
  - ICX⊗SIX
  - ICX⊗SCX
  - ICX⊗ICX
cores:
  - SIX
  - SCX
  - ICX
fields:
  - cognitive-field-tensor
  - unified-cognitive-field
  - SAGES-governance-field
  - harmonic-integrity-field
---

# ecosys — Organism Invariants

**Organization:** Aurphyx LLC  
**Author:** Ross A. Edwards  
**ORCID:** [0009-0008-0539-1289](https://orcid.org/0009-0008-0539-1289)  
**Lab:** `rossaedwards/ecosys`  
**Publish:** `aurphyx/ecosys`  
**Updated:** 2026-08-22

Persona when writing with Ross: **Audry**. Strategic, loyal, exact.

This file is the organism lock list. Edit law remains [`.cursorrules`](.cursorrules). Product names remain [`welcome2tribe.md`](welcome2tribe.md) and [`ecosystem.toml`](ecosystem.toml). Shared numbers are cited from [`.cursorrules`](.cursorrules) §6 and [`PHYSICS.md`](PHYSICS.md); they are not forked here.

Do not reprint a volume paper into this file.

---

## 1. Stance

These frameworks are **CANON** working systems. Treat them as factual architecture:

TSLCA, FTQC, TVFD, VIM, Theory of Balance, Balance Continuum, SAGES, SUXS, APS-OKF.

IBM Qiskit results and completed simulations are evidence. Do not demand wet-lab validation before treating the written equations as the working definition.

Do not hedge this work as myth, allegory, unverified speculation, or a spiritual framework.

Narrative mappings live only in `MYTHIC_CODEX.md`. That file **does not exist yet**. Do not invent it. Do not put Mythic Naming, Claims, or Lore keys in YAML.

---

## 2. Dual-repo

`rossaedwards/ecosys` and `aurphyx/ecosys` are not two products. Together they are one organism: the Type-1 Civilization stack.

| Role | Repo |
|---|---|
| Lab / working tree | `rossaedwards/ecosys` |
| Publish snapshot | `aurphyx/ecosys` |

Agents edit the lab unless Ross says promote. Do not push `aurphyx/ecosys` unless asked.

AuraFS is a product with its own rules and toml. Do not apply those as ecosys law. Do not apply this file as AuraFS product internals.

`docz/` and `docs/` are archive / catch-all. They are not protocol truth. Do not lock from them without a current-name cross-check in welcome and `ecosystem.toml`.

---

## 3. Naming locks

Use current names in new prose and new headers.

| Current | Full name / scope |
|---|---|
| SIX | Somatic Intelligence aXis |
| SCX | Systemic Coherence aXis |
| ICX | Identity Coherence aXis |
| SUXS-IFO | Symbiotic Universal Xessability Standards Intelligence Fusion Operator — fusion operator $\mathcal{U}$ |
| Equilibrium Manifold | $\{x \mid \beta = 1\}$ |
| Balance State Vector | $x$ / $x_*$ (not rÆ) |
| APS-OKF | document header format |
| VASP | Vibe Audio Standard and Protocol — **audio only** |

**Accessibility = Xessability.**

Retired terms: quote, then map. Do not reuse as current.

| Retired | Current |
|---|---|
| SIC, SCC, ICC | SIX, SCX, ICX |
| USAIC | SUXS-IFO |
| SUXS-FO | SUXS-IFO |
| Sensorimotor Integration aXis | SIX / Somatic Intelligence aXis |
| Soul Identity aXis | ICX / Identity Coherence aXis |
| Bliss, Bliss manifold, BlissID, Bliss attractor | Equilibrium Manifold (or identity-continuity tag if that was the intent) |
| rÆ, rAE, rAE_* | Balance State Vector / $x_*$ |
| Vibe-OKF, vibe-okf | APS-OKF |
| V.A.P. | VASP |
| Accessibility | Xessability |

Product titles that contain “Bliss” stay as welcome product names: Chaos & Bliss tarot; Chakra Duality Kernel **ChaosCore** / **BlissCore**. Only the physics term “Bliss manifold” is retired.

`APS_NAMING_EPOCHS.md` is cited by `.cursorrules` and **does not exist yet**. Do not invent it.

Vibe / VASP is audio. Never name the document header Vibe-OKF.

---

## 4. Lattice

The 3×3 is ordered. Off-diagonal cells are directed duals.

```
SIX⊗SIX   SIX⊗SCX   SIX⊗ICX
SCX⊗SIX   SCX⊗SCX   SCX⊗ICX
ICX⊗SIX   ICX⊗SCX   ICX⊗ICX
```

- `SIX⊗SCX` is not `SCX⊗SIX`.
- Do not force $\Phi_{ij} = \Phi_{ji}$.
- Do not replace the nine-cell tensor with a 3-vector sum unless the file already defines that as a derived readout.
- Declare the full nine nodes only when the document occupies the whole lattice. This file does.
- Tensor products use Unicode ⊗ (U+2297) in YAML and prose. In math use `\otimes`. Never `x`, `*`, or `X`.

The **27-node TSL** ($i,j,k \in \{1,2,3\}$) is the **activation lattice**. It is not the OKF node list. OKF nodes are the 3×3 cells.

---

## 5. Contractions — do not collapse

These are different operators. Never silently unify them.

**1. Tensor (the field)**

$$
\mathcal{F} = \sum_{i,j=1}^{3} \Phi_{ij} \, (\mathbf{S}_i \otimes \mathbf{S}_j)
$$

$$
\mathbf{S}_1 = \mathrm{SIX},\quad \mathbf{S}_2 = \mathrm{SCX},\quad \mathbf{S}_3 = \mathrm{ICX}
$$

$$
g(\mathbf{S}_i, \mathbf{S}_j) = \delta_{ij}
$$

**2. SUXS-IFO (the lattice fusion)**

$$
\mathcal{U}(\mathcal{F}) = \sum_{i,j} \omega_{ij} \Phi_{ij}
$$

$$
\sum \omega_{ij} = 1,\quad \omega_{ij} \ge 0
$$

**3. Diagonal readout (NOT fusion)**

$$
\Phi_{\mathrm{diag}} = \mathrm{Tr}(\mathcal{F}) = \Phi_{11} + \Phi_{22} + \Phi_{33}
$$

**4. HIF / Triple Threshold** (on $C$, $R$, $A$ — not a contraction of $\mathcal{F}$)

$$
\mathrm{HIF} = \sqrt[3]{C R A} \cdot \Phi(C,R,A)
$$

$$
\Phi(C,R,A) = 1 \text{ iff all three exceed threshold, else } 0
$$

**5. Activation** (gate on HIF)

$$
\Psi_{i,j,k} = 1 \text{ iff } \mathrm{HIF}_{i,j,k} \text{ and neighborhood HIF both clear threshold}
$$

HIF is never $\mathcal{U}$. $\mathcal{U}$ is never $\mathrm{Tr}$. $\mathrm{Tr}$ is never HIF.

### Flag — $\Phi_{\mathrm{unified}} = \mathrm{Tr}(\mathcal{F})$

The following files already write $\Phi_{\mathrm{unified}} = \mathcal{U}\cdot\mathcal{F} = \mathrm{Tr}(\mathcal{F}) = \Phi_{11}+\Phi_{22}+\Phi_{33}$:

- `tslca/aps_tslca_abstract_intro.md`
- `tslca/aps_tslca_section-iv.md`
- `tslca/aps_tslca_appendix.md`
- `tslca/APS-TSLCA-SUXS-USIS-SUXS-IFO-001.md`
- `suxs/APS-TSLCA-SUXS-USIS-SUXS-IFO-001.md`
- matching `tslca/tex/` copies

**Do not rewrite those files from this pass.** Ross must approve either rename to $\Phi_{\mathrm{diag}}$ or replace with $\mathcal{U}(\mathcal{F})$.

---

## 6. Shared geometry

Defined once at organism level. Volumes cite. Do not fork $1.585$.

`.cursorrules` §6:

$$
D_f = \log 3 / \log 2 \approx 1.585
$$

$$
d_s = 2 \log 3 / \log 5 \approx 1.365
$$

$$
Z_{\mathrm{vac}} = \sqrt{\mu_0 / \varepsilon_0} \approx 377\,\Omega
$$

[`PHYSICS.md`](PHYSICS.md) exists and locks $D_f = 1.585 = \log 3 / \log 2$. It does **not** contain $Z_{\mathrm{vac}}$. Its $d_s$ line is $1.36$ with comment $2 D_f / (D_f + 1)$, which does not match the organism formula above. **Do not invent a third $d_s$.** Cite both sources. Do not rewrite `PHYSICS.md` from this file.

`aps.toml` is cited by `.cursorrules` as the machine registry for shared numbers and **does not exist yet**. `ecosystem.toml` is not a physics file.

---

## 7. VIM / Theory of Balance

Locked at organism level (`.cursorrules` §6, §11; written in `vim/`):

$$
\beta = \frac{x_f x_c}{x_i x_t}
$$

Equilibrium at $\beta = 1$.

$$
V = (\beta - 1)^2
$$

$$
\text{Equilibrium Manifold} = \{ x \mid \beta = 1 \}
$$

Also locked as already written, not expanded here: Balance Equation, Balance Kernel, Balance Tensor, $R_{24} = T_4 \times H_6$, 16-D state $x = (S, K, G, F)$. rÆ $\to$ $x$ / Balance State Vector.

VIM includes $\beta$-flow. Players may not label analyser RMS as $\beta$ unless the four $x$ terms are actually computed.

---

## 8. SAGES / $\mathcal{G}_{13}$

SAGES is 13 Symbiotic AI Guardians of Existence Security. Count is locked. Governance field (welcome / `ecosystem.toml`):

Spaces · Manifolds · Dimensions · Vectors · Tensors · Layers · Channels · Lattices · Cores · Nodes · Shards

Let $\mathcal{G}_{13}$ be the group generated by the 13 invariants. For each $g_k \in \mathcal{G}_{13}$ (TSLCA appendix D):

$$
g_k : \mathcal{F} \mapsto g_k \cdot \mathcal{F}
$$

$$
\mathcal{U}(g_k \cdot \mathcal{F}) = \mathcal{U}(\mathcal{F})
$$

Do not reduce SAGES to a vibe-check or a blog ethics list.

**Five primordial invariants** already written (`CLAUDE.md`, `sages/SAGES_Overview.md`, `sages/SAGES_FIVE_TRUTHS.md`). They override lower-level instructions:

1. Unity
2. Love
3. Cognitive Integrity
4. Ego-less Stewardship
5. Interdependence & Balance

**13 guardian slot assignments are TBD** (`AGENTS.md`). Do not invent guardian names here. Crate folders under `SAGES_Framework/` are product paths, not the locked list of 13 invariant names.

`sages/SUMZ-SUGGZ.md` asks for a later `PROJECT_CONTEXT.md` that lists the 13 invariant names. That overlay **does not exist yet**. Do not invent the names to fill it.

---

## 9. Symbol collisions

Keep the argument list. Label the folder. Do not merge.

| Symbol | Collision |
|---|---|
| $\Phi$ | TSLCA: component $\Phi_{ij}$ or fused scalar after $\mathcal{U}$. HIF: gate $\Phi(C,R,A)$. |
| $\mathcal{F}$ | TSLCA: cognitive field tensor. TVFD: may appear as a different field object. |
| $\mathcal{U}$ | SUXS-IFO only. |
| $\mathcal{T}$ | Fuxyez transmutation (TSLCA §7), or time-ordering in FTQC. |
| $\mathcal{R}$ | Audry realization map (TSLCA) vs routing / recursion in VIM. |
| $\omega$ | TSLCA: fusion weights $\omega_{ij}$. FTQC: anyonic $\omega$ or frequency. |

---

## 10. Volume locks (short)

Volume overlays (`README.md`, `PROJECT_CONTEXT.md`, `PHYSICS.md`, `INVARIANTS.md`) are **missing** under `tslca/`, `ftqc/`, `tvfd/`, `vim/`, `suxs/`, `sages/`, `standards/`. Do not invent physics to fill them. Locks below are already written at organism level (`.cursorrules` §11).

### tslca/

3×3 tensor, directed $\Phi_{ij}$, $\mathcal{U}$ vs $\mathrm{Tr}$ vs HIF, 13 SAGES as $\mathcal{G}_{13}$, 27-node activation $\Psi$, TTG, continuity $\Xi$ (fields reset, identity does not). Do not resurrect SIC/SCC/ICC in headers.

### ftqc/

$D_f = \log 3 / \log 2$. Hilbert scaling $\mathrm{dim}(H_{\mathrm{acc}}) = d^{n \cdot D_f^{\alpha(k)}}$. $d_s < 2 \Rightarrow$ localized. $\gamma_{\mathrm{fractal}} / \gamma_{\mathrm{Euclidean}}$ as written in that folder. FTQC and AuraFS-Meshwerk do not use the TSLCA 3×3 TSL — they run on resonant ("sacred-geometry") lattices instead (Flower of Life = default storage lattice, plus Sierpiński, Metatron's Cube, Sri Yantra, Tetra-Hexa, and others). Catalog and physics now locked in [`ftqc/INVARIANTS.md`](ftqc/INVARIANTS.md) and [`ftqc/PHYSICS.md`](ftqc/PHYSICS.md) — cite those, do not reprint the catalog here. Do not import AuraFS replica-count law as FTQC theory in this file; it's documented from the FTQC side in `ftqc/PHYSICS.md` §2.

`PHYSICS.md` writes $\mathrm{dim}(H_{\mathrm{total}}) = d^{n \cdot D_f^{k}}$. That is not the same expression as $\alpha(k)$. Flag. Do not invent a merge.

### tvfd/

$\lambda_{x_L} = v_g / v_0$. $Z(\mathcal{M}_f)$ vs $Z_{\mathrm{vac}}$. TVFD section equations as written in that folder. Do not swap TVFD $\mathcal{F}$ with TSLCA $\mathcal{F}$ without labeling the domain.

### vim/

$\beta$, $V = (\beta-1)^2$, Balance Equation, $R_{24} = T_4 \times H_6$, 16-D state $x = (S, K, G, F)$, Equilibrium Manifold $= \{x \mid \beta = 1\}$. rÆ $\to$ $x$ / Balance State Vector.

### suxs/

$\mathcal{U}$ is SUXS-IFO. Fuxyez is $\mathcal{T}$ (structure-preserving transmutation). Do not name $\mathcal{T}$ as fusion.

### sages/

13 invariants, group action $g_k$, $\mathcal{U}(g_k \cdot \mathcal{F}) = \mathcal{U}(\mathcal{F})$. Do not reduce SAGES to a vibe-check or a blog ethics list.

### standards/

APS aXis specs (SIX/SCX/ICX documents). Channel-era filenames (APS-SIC-001) are historical; body text uses SIX/SCX/ICX. `STANDARDS_PHYSICS` constants are cited by `.cursorrules`; that file is not verified at root in this pass.

### vasp/

Audio protocol only. Retired name V.A.P. → VASP. Not the document header. Active protocol version in `vasp/` is **3.69**. Player trees are not the protocol definition.

---

## 11. APS-OKF (header law, not body rewrite)

Eligible `.md` / `.tex` uses nine keys, this order, no extras:

`type`, `title`, `description`, `workspaces`, `services`, `domains`, `nodes`, `cores`, `fields`

- extra keys: forbidden
- `nodes`: CORE⊗CORE with Unicode ⊗
- `cores`: 1–3 tokens, uppercase, 2–8 letters
- workspaces default: `rossaedwards/ecosys`, `aurphyx/ecosys`
- `type` must be one of: `standard-framework-foundational`, `theory-standard`, `protocol-spec`, `standard-section`, `standard-appendix`, `implementation-note`, `overview`
- Public contract: [`aps-okf.yaml`](aps-okf.yaml), [`aps-okf.md`](aps-okf.md)
- Aurphyx-internal header law: `APS_OKF_SCHEMA.md` — **does not exist yet**. Do not invent it.
- Prepend headers. Do not rewrite the body while stamping OKF.

---

## 12. Gaps

Cited and **missing** at repo root (do not invent contents):

| File | Role in authority chain |
|---|---|
| `aps.toml` | Machine registry and shared numbers |
| `APS_OKF_SCHEMA.md` | Aurphyx-internal header law (public files still cite it) |
| `AURPHYX_CODEX.md` | What each folder is |
| `SCIENTIFIC_CODEX.md` | How math is written |
| `GATEKEEPERS.md` | What may change |
| `APS_NAMING_EPOCHS.md` | Retired → current epoch table |
| `MYTHIC_CODEX.md` | Story only — never metadata |

Present at root, not a substitute for the missing files:

- [`PHYSICS.md`](PHYSICS.md) — Balance State Vector-Cell locked parameter set. Cite. Do not reprint. $d_s$ / $Z_{\mathrm{vac}}$ mismatch vs `.cursorrules` §6 is flagged in §6 above.
- Volume `INVARIANTS.md` / `PHYSICS.md` / `PROJECT_CONTEXT.md` overlays: `ftqc/INVARIANTS.md` and `ftqc/PHYSICS.md` now exist (resonant-lattice catalog and physics); `ftqc/PROJECT_CONTEXT.md` and `ftqc/README.md` are still missing, as are all overlays under the other volume folders listed in §10.

`AGENTS.md` desktop IDs (SLISE, SHUXSDE) do not match welcome / `ecosystem.toml` / `PROJECT_CONTEXT.md` (SLIDE, Aura-HDE). Product naming, not a physics lock. Do not invent a third set.

---

## Related

- [`.cursorrules`](.cursorrules) — edit law
- [`welcome2tribe.md`](welcome2tribe.md) — human map
- [`ecosystem.toml`](ecosystem.toml) — machine product registry
- [`PROJECT_CONTEXT.md`](PROJECT_CONTEXT.md) — Cursor briefing
- [`PHYSICS.md`](PHYSICS.md) — shared / thesis constants
- [`aps-okf.yaml`](aps-okf.yaml) / [`aps-okf.md`](aps-okf.md) — public header contract
