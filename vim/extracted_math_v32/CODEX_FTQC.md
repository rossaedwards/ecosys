## ** APS‑EXT-MATH-FTQC‑CODEX **
## ** Version 1.0 (Draft) **
## ** Aurphyx Primordial Standards **
## ** Ross A. Edwards | Aurphyx LLC | SUXS **
## ** SAGES | AGPLv3 **

# FTQC
Found: 553 unique items
---
**inline**   ftqc\appendix_ix_unified.tex
```latex
$O(\log N)$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$C \propto N^{D_f}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$D_f = 1.585$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$C \propto N$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$16\times$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$O(\log_{3} N)$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$k = 0, 1, 2, 3, 4$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\dim(\mathcal{H}_{\text{acc}}) = 2^{n \cdot D_f^{\alpha_k}}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$7.1\times$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$10^4\times$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\epsilon_i \in [-W/2, W/2]$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$W=0.3t$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\pm 3\%$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$N=1093$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$|\psi(x)|^2$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\sim e^{-x/\xi}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\xi \approx 0.3L$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$d_s = 1.36 < 2$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\xi/L = 0.3$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\xi/L \approx 0.8$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$T_2 = 50\,\mu$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$e^{-t/T_2}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$T_2 = 200\,\mu$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$T_2 = 1150\,\mu$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$23\times$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\gamma_\phi = 1/T_2$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\text{IPR} = 0.016$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\text{IPR} = 0.21$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\gamma_{\text{fractal}}/\gamma_{\text{Euclidean}} \approx 0.063$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$N=123$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$3.2\times$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$d^{n \cdot D_f^{\alpha_k}}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$D_f = \log 3 / \log 2 \approx 1.585$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\alpha_k$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{H}_{\text{acc}}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{C}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$d_\chi = 0$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$R_{\chi,a}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$d_s = 1.365$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\Gamma_{\text{r}\Gamma}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\Gamma$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$Z_{\mathcal{M}_f}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$Z_{\text{vac}} = \sqrt{\mu_0/\epsilon_0} \approx 377\,\Omega$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\eta = 1 - |Z_{\mathcal{M}_f} - Z_{\text{vac}}|^2 / 4Z_{\mathcal{M}_f}Z_{\text{vac}}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$_{6v}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\lambda_{\text{lyterL}}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{F}_{ij}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$i,j \in \{1,2,3\}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$3 \to 6 \to 9 \to 13$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{A}_{\text{UAST}}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$3 \times 3$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{U}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{U}: \mathcal{F} \to \Psi$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\Psi(x) = \sum_{i,j=1}^{3} \omega_{ij} \mathcal{F}_{ij}(x)$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\omega_{ij}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\sum_{i,j} \omega_{ij} = 1$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{A}_{\alpha \to \beta}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\alpha$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\beta$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{G}_{13}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_k$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$k \in \{1, \ldots, 13\}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_1$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_2$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_3$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_4$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_5$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_6$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_7$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_8$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_9$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_{10}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_{11}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_{12}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathbb{I}_{13}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$O(\log_3 N)$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\text{ChaosCore} \otimes \text{BlissCore}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$d^{n D_f^{\alpha_k}}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{H}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\mathcal{F}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\text{acc}$
```

**inline**   ftqc\appendix_ix_unified.tex
```latex
$\text{vac}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$T_2 < 100~\mu$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\sim 1000:1$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\Df = \log 3/\log 2 \approx 1.585$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\dim(\Hilbert_{\mathrm{acc}}) = 2^{n \cdot \Df^{\alpha(k)}}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$n = 12$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$d_\omega = 0$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$C_{6v}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\gamma_{\mathrm{fractal}}/\gamma_{\mathrm{Euclidean}} = 0.063$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\times$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$n = 5$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$T_2 \sim 100~\mu$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$T_2 \sim 1$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$> 99.9\%$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$d_s < d$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$d_s \approx 1.36$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$d_s^c = 2$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$k = 4$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\sim 100$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\{v_1, v_2, v_3\}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$S_{k+1}$
```

**env:align**   ftqc\arxiv_ftqc_complete.tex
```latex
\begin{align}
N(k) &= \frac{3^{k+1} + 3}{2} \text{ vertices}, \\
E(k) &= 3^{k+1} \text{ edges}, \\
\Df &= \frac{\log 3}{\log 2} \approx 1.585 \text{ (Hausdorff dimension)}.
\end{align}
```

**env:equation**   ftqc\arxiv_ftqc_complete.tex
```latex
\begin{equation}
d_s = \frac{2 \log 3}{\log 5} \approx 1.365.
\end{equation}
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\Hilbert_n$
```

**env:equation**   ftqc\arxiv_ftqc_complete.tex
```latex
\begin{equation}
\dim(\Hilbert_{\mathrm{acc}}) = d^{n \cdot \Df^{\alpha(k)}},
\label{eq:main_theorem}
\end{equation}
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$d = 2$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\Df = \log 3 / \log 2$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\alpha(k) = 1 - c/k$
```

**env:equation**   ftqc\arxiv_ftqc_complete.tex
```latex
\begin{equation}
\mathcal{A}(n, k) = \frac{\dim(\Hilbert_{\mathrm{acc}}^{\mathrm{fractal}})}{\dim(\Hilbert^{\mathrm{Euclidean}})} = 2^{n(\Df^{\alpha(k)} - 1)}.
\end{equation}
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$k = 3$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\mathcal{A} \approx 10^4$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$R_z(0.95\pi)$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\rightarrow$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$2^{5 \times 0.585} \approx 7$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\Df - 1 = 0.585$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\{a, b, c, \ldots\}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$a \otimes b = \bigoplus_c N_{ab}^c \, c$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$d_a > 0$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\omega$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$R_{\omega,a} \neq 1$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\omega \otimes a \neq 0$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\overline{U}_q(\mathfrak{sl}_2)$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$q = e^{i\pi/k}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$R_{\omega,a}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$(\mathfrak{sl}(2), k=2)$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$R_{\omega, V_1} = e^{i\pi/4}$
```

**env:equation**   ftqc\arxiv_ftqc_complete.tex
```latex
\begin{equation}
D_{\mathrm{ground}} \sim \mathcal{D}^{2g \cdot \Df^{\alpha}(k)},
\end{equation}
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\mathcal{D}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$(sl(2), k=2)$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$D_{\mathrm{ground}} \approx 100$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$25\times$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\omega \in [2.50, 3.10] \times 2\pi c/a$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\Delta\omega/\omega_{\mathrm{mid}} = 21\%$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$v_g < 0.01c$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$d_s < 2$
```

**env:equation**   ftqc\arxiv_ftqc_complete.tex
```latex
\begin{equation}
\frac{\gamma_{\mathrm{fractal}}}{\gamma_{\mathrm{Euclidean}}} \approx 0.063,
\end{equation}
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\sim 16\times$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$v_g \approx 0.08c$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\xi_{\mathrm{edge}} \approx 2a$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\delta r/r < 10\%$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\tau_{\mathrm{fractal}}/\tau_{\mathrm{flat}} \geq 3.2\times$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$k = 0$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$^{15}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$T_2(k=3)/T_2(\mathrm{flat}) \geq 2.0$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$^{171}$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$D_{\mathrm{eff}}^{\mathrm{fractal}}/D_{\mathrm{eff}}^{\mathrm{linear}} \geq 10\times$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$a = 15~\mu$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$F_g^{\mathrm{T}}/F_g^{\mathrm{linear}} \geq 1.5\times$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\sim 10^6$
```

**inline**   ftqc\arxiv_ftqc_complete.tex
```latex
$\sim 6 \times 10^4$
```

**inline**   ftqc\FTQC_INTEGRATION_GUIDE.md
```latex
$1.25M
- Timeline: 18 months to TRL 4
- Remove: All detailed fabrication parameters, step-by-step measurement procedures, individual budget breakdowns, risk matrices

### PRX Figure Selection (Maximum 4 Figures)

**Required figures:**
1. Figure 1: Hilbert Scaling (shows 7x advantage at n=5, extrapolates to 10^4x at n=12)
2. Figure 2: Coherence Dynamics (demonstrates 16x improvement)
3. Figure 3: A…
```

**inline**   ftqc\FTQC_INTEGRATION_GUIDE.md
```latex
$20-50M for Phase 3

**For Optica:** Replace qubit count with "photonic cavity count" and focus on integrated photonics scaling.

### Subsection 4: Technology Readiness Projection

**Purpose:** Provide realistic commercialization timeline.

**Content (1 paragraph):**

"The proposed experimental program advances the fractal quantum computing architecture from its current Technology Readiness Level …
```

**inline**   ftqc\main.tex
```latex
$\Df \approx 1.585$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\tau < \SI{100}{\micro\second}$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$10^{4}\times$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\Df = 1.585$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$2^{n \cdot \Df} \approx 2^{19}$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$2^{12}$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\Delta\omega = 0.4 \times 2\pi c/a$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\gamma_{\mathrm{fractal}}/\gamma_{\mathrm{Euclidean}} = 0.63$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$O(\text{diam})$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\text{diam}$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$10^{-12}$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$d \geq 27$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$2d^2 \approx 1458$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$2.9\%$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\Df = \log 3 / \log 2 \approx 1.585$
```

**env:equation**   ftqc\prx_ftqc_submission.tex
```latex
\begin{equation}
\rho(E) \propto E^{d_s/2 - 1},
\label{eq:dos_fractal}
\end{equation}
```

**env:equation**   ftqc\prx_ftqc_submission.tex
```latex
\begin{equation}
\dim(\Hilbert_{\mathrm{total}}) = d^{n \cdot \Df^{\alpha(k)}},
\label{eq:hilbert_scaling}
\end{equation}
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\alpha(k)$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$10^{4}$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$a = 2r$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\omega_1 = 2.5\pi c/a$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\omega_2 = 3.1\pi c/a$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\gamma_{\mathrm{fractal}}/\gamma_{\mathrm{Euclidean}} \approx 0.63$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\nu = 5/2$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$(s\ell(2), k)$
```

**env:equation**   ftqc\prx_ftqc_submission.tex
```latex
\begin{equation}
D_{\mathrm{ground}}^{\mathrm{fractal}} \sim 
\left( \sum_a d_a^2 \right)^{\Df},
\label{eq:ground_degeneracy}
\end{equation}
```

**env:equation**   ftqc\prx_ftqc_submission.tex
```latex
\begin{equation}
\mathcal{R}: \mathcal{B}(\Hilbert_{\mathrm{system}}) \to 
\mathcal{B}(\Hilbert_{r\AE}),
\label{eq:rae_channel}
\end{equation}
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\mathcal{B}(\Hilbert)$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\Hilbert$
```

**env:equation**   ftqc\prx_ftqc_submission.tex
```latex
\begin{equation}
\mathcal{R}(\rho) = \sum_{k} E_k \rho E_k^\dagger, 
\quad \sum_k E_k^\dagger E_k = \mathbb{I},
\label{eq:kraus}
\end{equation}
```

**env:equation**   ftqc\prx_ftqc_submission.tex
```latex
\begin{equation}
E_k = E_k^{(0)} + \lambda \mathcal{Z}_k,
\label{eq:casimir_param}
\end{equation}
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\mathcal{Z}_k$
```

**inline**   ftqc\prx_ftqc_submission.tex
```latex
$\lambda$
```

**inline**   ftqc\rae-ftqc_optica_submission_FINAL.tex
```latex
$d_s = 1.36$
```

**inline**   ftqc\rae-ftqc_prx_submission_FINAL.tex
```latex
$2^{n \cdot \Df^{\alpha(k)}}$
```

**inline**   ftqc\rae-ftqc_prx_submission_FINAL.tex
```latex
$\omega a/2\pi c \in [2.5, 3.1]$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{1, \sigma, \psi\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$d_\sigma = \sqrt{2}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{1, \tau\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$d_\tau = \phi$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{1, e, m, \epsilon\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$d_a = 1$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathcal{D} = \sqrt{\sum_a d_a^2}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{a_1, \ldots, a_r\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$d_{a_i} > 0$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{P_1, \ldots, P_s\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{\omega_1, \ldots, \omega_t\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$d_{\omega_j} = 0$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\omega \in \mathcal{C}$
```

**env:equation**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
\begin{equation}
d_\omega = \mathrm{Tr}(\mathrm{id}_\omega) = 0,
\label{eq:neglecton_dim}
\end{equation}
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$R_{\omega, a}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\omega \otimes a$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$(s\ell(2), k=2)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathfrak{sl}(2)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$k = 2$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{V_0, V_1, V_2\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\{1, \sqrt{2}, 1\}$
```

**env:align**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
\begin{align}
V_1 \otimes V_1 &= V_0 \oplus V_2, \\
V_1 \otimes \omega &= \omega, \\
\omega \otimes \omega &= 0.
\end{align}
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$R_{\omega, a} \neq \pm 1$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathrm{SU}(N)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$G \subset \mathrm{SU}(N)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$e^{i\theta}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\theta/\pi$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\epsilon$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$O(\log^c(1/\epsilon))$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$G \cup \{R_{\omega, a}\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\theta = \pi/4$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$N = 5$
```

**env:equation**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
\begin{equation}
H_{\mathrm{int}} = \sum_{\langle i,j \rangle} J_{ij} \sigma_i^x \sigma_j^x 
+ \sum_{k} \Phi_{\mathrm{Berry}}^{(k)} \sigma_k^z,
\label{eq:fractal_hamiltonian}
\end{equation}
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$J_{ij}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$(i, i+1)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$i \in \{0, 1, 2, 3\}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$(0 \to 2)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$(1 \to 4)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\ell = 1$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\Phi_{\mathrm{Berry}} = 0.95\pi$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathrm{Tr}(\rho^2)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$F = |\langle \mathrm{GHZ}|\psi\rangle|^2$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$2^5 = 32$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$2^{5 \times 1.585} \approx 227$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$2^{N \cdot \Df} / 2^N = 2^{N(\Df - 1)}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$N = 12$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$2^{12 \times 0.585} 
\approx 10^{2.1}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$k = 1$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathcal{D}^2 = \sum_a d_a^2$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathcal{L}_k$
```

**env:equation**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
\begin{equation}
D_{\mathrm{ground}}^{\mathrm{fractal}} \sim 
\mathcal{D}^{2g \cdot \Df^{\alpha(k)}},
\label{eq:fractal_degeneracy}
\end{equation}
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathcal{D}^2 = 4$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$g = 1$
```

**env:equation**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
\begin{equation}
D_{\mathrm{ground}} \sim 4^{1.585^{2.38}} \approx 4^{3.28} \approx 100.
\end{equation}
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$D = 4$
```

**env:equation**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
\begin{equation}
s_\omega(E) = \mathrm{Tr}_{\mathcal{H}_{\mathrm{code}}}
(P_\omega E P_\omega^\dagger),
\label{eq:neglecton_syndrome}
\end{equation}
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$P_\omega$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$2\times$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\sim 10^3$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$3\times$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\sim 10^2$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$5\times$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$10\times$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\sim 10^1$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\mathbf{16\times}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\sim 3\times$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\sim 2\times$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\sim 2.7\times$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$2^{N \cdot 1.585}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$5.8 \times 10^1$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\langle\sigma_x\rangle \propto e^{-\gamma t}$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\gamma = 0.23$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\gamma = 0.05$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\gamma = 0.01$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$k = 6$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$|\psi| \propto \exp(-x/\xi)$
```

**inline**   ftqc\section_iii_non_semisimple_tqft.tex
```latex
$\xi \approx 0.3 L$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$k \in \mathbb{N}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$(V_k, E_k)$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\mathcal{T}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\mathcal{L}_0$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\mathcal{L}_k = \mathcal{T}^k(\mathcal{L}_0).
\end{equation}
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
|V_k| = |V_0| \cdot r^{k \cdot \Df},
\label{eq:hausdorff_def}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\mathcal{S}_k$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\mathcal{S}_0$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$|V_0| = 3$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
|V_k| = \frac{3(3^k + 1)}{2}.
\end{equation}
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\Df = \frac{\log 3}{\log 2} \approx 1.585.
\label{eq:sierpinski_dim}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\mathcal{L} = (V, E)$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$n = |V|$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\Hilbert_{\mathrm{total}} = \bigotimes_{v \in V} \Hilbert_v, \quad 
\dim(\Hilbert_v) = d.
\end{equation}
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\dim(\Hilbert_{\mathrm{total}}) = d^n.
\label{eq:standard_dim}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\mathcal{L}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$|\psi_0\rangle$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\Hilbert_{\mathrm{acc}}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\Hilbert_{\mathrm{acc}} = \mathrm{span}\left\{ e^{-iHt} |\psi_0\rangle : 
t \in [0, T] \right\}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\dim(\Hilbert_{\mathrm{acc}})$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
H = -t \sum_{\langle i,j \rangle \in E} \left( c_i^\dagger c_j + 
c_j^\dagger c_i \right) + \sum_{i \in V} \epsilon_i c_i^\dagger c_i,
\label{eq:tight_binding}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\epsilon_i$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
N(E) \propto E^{d_s/2}, \quad E \to 0^+,
\label{eq:idos}
\end{equation}
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\rho(E) = \frac{dN}{dE} \propto E^{d_s/2 - 1}.
\label{eq:dos}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$d_s = 2\log 3 / \log 5 \approx 1.365$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
P(t) \sim t^{-d_s/2}, \quad t \to \infty.
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$P(t) = \int \rho(E) e^{-Et} dE$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\rho(E) \propto E^\alpha$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\alpha = d_s/2 - 1$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$d_s = 2\log 3 / \log 5$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$d_s/2 - 1 < 0$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$E \to 0$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\ell \leq k$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$3^\ell$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\mathcal{L}_{k-\ell}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$|\Omega\rangle$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
S_A \propto |\partial A|^{(\Df - 1)/\Df} \cdot \ell,
\label{eq:entropy_scaling}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$|\partial A|$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$S_A \propto |\partial A|$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$n = |V_k|$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\alpha(k) = \frac{\log(1 + k \cdot \eta)}{\log \Df}
\label{eq:alpha_def}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\eta \in (0, 1]$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\eta \to 1$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$k \gg 1$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\dim(\Hilbert_{\mathrm{acc}}) \approx d^{n \cdot k}.
\label{eq:strong_coupling}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$3^{\ell}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\ell = 0, 1, \ldots, k$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\Hilbert^{(\ell)} = \bigotimes_{j=1}^{3^\ell} \Hilbert_j^{(k-\ell)},
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\Hilbert_j^{(k-\ell)}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$k - \ell$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
H = \sum_{\ell=0}^{k} H^{(\ell)} + \sum_{\ell=0}^{k-1} V^{(\ell, \ell+1)},
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$H^{(\ell)}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$V^{(\ell, \ell+1)}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\| V^{(\ell, \ell+1)} \| \leq \eta \cdot \| H^{(\ell)} \|.
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\ell + 1$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\ell = 0$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$d^{n_0}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$n_0 = |V_0|$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$n_\ell = n_0 \cdot r^{\ell \cdot \Df}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\dim(\Hilbert_{\mathrm{acc}}) = \prod_{\ell=0}^{k} 
\left( d^{n_\ell} \right)^{\eta^\ell} 
= d^{\sum_{\ell=0}^{k} n_\ell \cdot \eta^\ell}.
\end{equation}
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\sum_{\ell=0}^{k} n_\ell \cdot \eta^\ell = n_0 \sum_{\ell=0}^{k} 
(r^{\Df} \cdot \eta)^\ell.
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$r^{\Df} \cdot \eta > 1$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$r = 2$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\eta > 0.33$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$(r^{\Df} \cdot \eta)^k$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$n = n_0 \cdot r^{k \cdot \Df}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\dim(\Hilbert_{\mathrm{acc}}) \approx d^{n \cdot \Df^{\alpha(k)}}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\eta \approx 0.5$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\mathcal{A}(n, k) = \frac{\dim(\Hilbert_{\mathrm{acc}}^{\mathrm{fractal}})}
{\dim(\Hilbert_{\mathrm{acc}}^{\mathrm{Euclidean}})} 
= 2^{n(\Df^{\alpha(k)} - 1)}.
\label{eq:advantage}
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\dim(\Hilbert_{\mathrm{acc}}) = d^n$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\eta = 0.7$
```

**env:align**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{align}
\alpha(3) &= \frac{\log(1 + 3 \times 0.7)}{\log 1.585} \approx 2.38, \\
\dim(\Hilbert_{\mathrm{acc}}^{\mathrm{fractal}}) &= 2^{12 \times 1.585^{2.38}} 
\approx 2^{12 \times 3.28} \approx 2^{39.4}.
\end{align}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$2^{12} = 4096$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\mathcal{A}(12, 3) = 2^{39.4 - 12} = 2^{27.4} \approx 1.8 \times 10^8.
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$n = 100$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$k = 5$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\eta = 0.8$
```

**env:align**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{align}
\alpha(5) &\approx 3.02, \\
\dim(\Hilbert_{\mathrm{acc}}^{\mathrm{fractal}}) &\approx 2^{100 \times 1.585^{3.02}} 
\approx 2^{100 \times 4.16} \approx 2^{416}.
\end{align}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$2^{100}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\mathcal{A}(100, 5) = 2^{316} \approx 10^{95}.
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$10^{120}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\log_2 \dim(\Hilbert_{\mathrm{fractal}})$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\log_2 \dim(\Hilbert_{\mathrm{Euclidean}})$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\log_{10} \mathcal{A}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\xi(t) \sim t^{1/2}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\dim(\Hilbert_{\mathrm{acc}}^{\mathrm{Euclidean}}) \sim d^{\xi(t)^D} 
\sim d^{t^{D/2}},
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$\tau_\ell \sim r^{-\ell}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
\xi_{\mathrm{fractal}}(t) \sim t^{1/d_w},
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$d_w = \log(5)/\log(2) \approx 2.32$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$1/d_w < 1/2$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
R_{\mathrm{Euclidean}} \sim \left( \frac{\log(1/p_L)}{\log(1/p_{\mathrm{phys}})} 
\right)^2,
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$p_{\mathrm{phys}}$
```

**env:equation**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{equation}
R_{\mathrm{fractal}} \sim \left( \frac{\log(1/p_L)}{\log(1/p_{\mathrm{phys}})} 
\right)^{2/\Df}.
\end{equation}
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$2/1.585 \approx 1.26$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$p_{\mathrm{phys}} = 10^{-3}$
```

**inline**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
$p_L = 10^{-12}$
```

**env:align**   ftqc\section_ii_fractal_hilbert_scaling.tex
```latex
\begin{align}
R_{\mathrm{Euclidean}} &\approx 1458, \\
R_{\mathrm{fractal}} &\approx 89.
\end{align}
```

**env:align**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{align}
\mathbf{a}_1 &= a \hat{x}, \\
\mathbf{a}_2 &= \frac{a}{2} \hat{x} + \frac{a\sqrt{3}}{2} \hat{y},
\end{align}
```

**env:align**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{align}
\mathbf{b}_1 &= \frac{2\pi}{a} \hat{x} - \frac{2\pi}{a\sqrt{3}} \hat{y}, \\
\mathbf{b}_2 &= \frac{4\pi}{a\sqrt{3}} \hat{y}.
\end{align}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\Gamma = (0, 0)$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$K = \frac{2\pi}{a}\left(\frac{2}{3}, 0\right)$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$M = \frac{2\pi}{a}\left(\frac{1}{2}, \frac{1}{2\sqrt{3}}\right)$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$C_3^{-1}$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$C_6^{-1}$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\sigma_v$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\mathbf{E} = E_z(x,y) \hat{z}$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\nabla \times \left( \frac{1}{\epsilon(\mathbf{r})} \nabla \times \mathbf{H} \right) = \frac{\omega^2}{c^2} \mathbf{H}.
\label{eq:master}
\end{equation}
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
-\nabla \cdot \left( \frac{1}{\epsilon(\mathbf{r})} \nabla E_z \right) = \frac{\omega^2}{c^2} E_z.
\label{eq:tm_scalar}
\end{equation}
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
E_z(\mathbf{r}) = e^{i\mathbf{k} \cdot \mathbf{r}} \sum_{\mathbf{G}} c_{\mathbf{k}}(\mathbf{G}) e^{i\mathbf{G} \cdot \mathbf{r}},
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\mathbf{G}$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\mathbf{k}$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\frac{1}{\epsilon(\mathbf{r})} = \sum_{\mathbf{G}} \kappa(\mathbf{G}) e^{i\mathbf{G} \cdot \mathbf{r}}.
\end{equation}
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\sum_{\mathbf{G}'} |\mathbf{k} + \mathbf{G}| \, \kappa(\mathbf{G} - \mathbf{G}') \, |\mathbf{k} + \mathbf{G}'| \, c_{\mathbf{k}}(\mathbf{G}') = \frac{\omega^2}{c^2} c_{\mathbf{k}}(\mathbf{G}).
\label{eq:pwe_eigenvalue}
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$N_G \approx 500$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\epsilon = 1$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\epsilon_m = 2.13$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\epsilon(\mathbf{r}) = \begin{cases}
1 & |\mathbf{r} - \mathbf{R}_j| < r \text{ for any lattice site } \mathbf{R}_j, \\
\epsilon_m & \text{otherwise}.
\end{cases}
\end{equation}
```

**env:cases**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{cases}
1 & |\mathbf{r} - \mathbf{R}_j| < r \text{ for any lattice site } \mathbf{R}_j, \\
\epsilon_m & \text{otherwise}.
\end{cases}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$f = \pi r^2 / (\sqrt{3} a^2 / 2)$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$r/a = 0.35$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$f \approx 0.44$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\frac{\omega_{\mathrm{lower}}}{2\pi c/a} = 2.50, \quad \frac{\omega_{\mathrm{upper}}}{2\pi c/a} = 3.10.
\end{equation}
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\frac{\Delta\omega}{\omega_{\mathrm{mid}}} = \frac{\omega_{\mathrm{upper}} - \omega_{\mathrm{lower}}}{(\omega_{\mathrm{upper}} + \omega_{\mathrm{lower}})/2} = \frac{0.60}{2.80} \approx 0.21.
\label{eq:gap_width}
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\lambda \in [1420, 1760]$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\Delta\omega_{\mathrm{flat}} < 0.02 \times 2\pi c/a.
\end{equation}
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\rho(\omega) = \frac{dN}{d\omega} \propto \frac{1}{\sqrt{\omega - \omega_{\mathrm{flat}}}},
\label{eq:flatband_dos}
\end{equation}
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\omega(\mathbf{k}) = \omega_D \pm v_D |\mathbf{k} - \mathbf{K}|,
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\omega_D \approx 2.80 \times 2\pi c/a$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$v_D \approx 0.3c$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\gamma_n = i \oint_{\mathrm{BZ}} \langle u_{n\mathbf{k}} | \nabla_{\mathbf{k}} | u_{n\mathbf{k}} \rangle \cdot d\mathbf{k},
\label{eq:zak}
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$|u_{n\mathbf{k}}\rangle$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\gamma_{\mathrm{total}} = \pi$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\mathbf{a}_1/2$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\xi \propto \left( \frac{E - E_c}{W} \right)^{-\nu}, \quad \nu = \frac{1}{d_s - d_s^c},
\label{eq:loc_length}
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\gamma$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\gamma = \gamma_0 \cdot \mathrm{IPR}, \quad \mathrm{IPR} = \sum_i |\psi_i|^4,
\label{eq:dephasing}
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\gamma_0$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\psi_i$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\sim 1/N$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\sim 1$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\frac{\gamma_{\mathrm{fractal}}}{\gamma_{\mathrm{Euclidean}}} \approx \left( \frac{\xi_{\mathrm{fractal}}}{\xi_{\mathrm{Euclidean}}} \right)^{-d_s} \approx 0.063,
\label{eq:gamma_ratio}
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$W = 0.5t$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\gamma_{\mathrm{fractal}}/\gamma_{\mathrm{Euclidean}} = 0.058 \pm 0.012$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
\rho(\omega) \propto \begin{cases}
(\omega - \omega_{\mathrm{edge}})^{-1/2} & \text{band edge}, \\
\log|\omega - \omega_{\mathrm{saddle}}| & \text{saddle point}.
\end{cases}
\end{equation}
```

**env:cases**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{cases}
(\omega - \omega_{\mathrm{edge}})^{-1/2} & \text{band edge}, \\
\log|\omega - \omega_{\mathrm{saddle}}| & \text{saddle point}.
\end{cases}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\omega = 2.50 \times 2\pi c/a$
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
F_P = \frac{\rho(\omega)}{\rho_0} \approx 50,
\end{equation}
```

**env:equation**   ftqc\section_iv_photonic_band_engineering.tex
```latex
\begin{equation}
v_g = \frac{d\omega}{dk} \to 0,
\end{equation}
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$n_g = c/v_g > 100$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$n_g^2$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\ell = 2$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\delta r < 0.5~\mu$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$a = 500$
```

**inline**   ftqc\section_iv_photonic_band_engineering.tex
```latex
$\exp(-x/\xi)$
```

**inline**   ftqc\section_viii_impact.tex
```latex
\(D_{\mathrm{eff}} \sim 10^{125}\)
```

**inline**   ftqc\section_viii_impact.tex
```latex
\(n = 12\)
```

**display**   ftqc\section_viii_impact.tex
```latex
\[
\dim(\mathcal{H}_{\mathrm{fractal}}) \approx 5.2 \times 10^7
\]
```

**display**   ftqc\section_viii_impact.tex
```latex
\[
\dim(\mathcal{H}_{\mathrm{Euclidean}}) = 4096
\]
```

**env:equation**   ftqc\section_vii_conclusion.tex
```latex
\begin{equation}
\dim(\mathcal{H}_{\mathrm{acc}}) = d^{n \cdot D_f^{\alpha(k)}},
\end{equation}
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$10^{95}\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$d_s \approx 1.36 < 2$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$100\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$2.7\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$\sim 70\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$\gamma_{\mathrm{fractal}} / \gamma_{\mathrm{Euclidean}} \approx 0.063$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$\tau_{\mathrm{fractal}} / \tau_{\mathrm{flat}} \geq 3.2\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$D_{\mathrm{eff}}^{\mathrm{fractal}} / D_{\mathrm{eff}}^{\mathrm{linear}} \geq 10\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$\Delta\omega / \omega_{\mathrm{mid}} \approx 21\%$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$F_g^{\mathrm{T}} / F_g^{\mathrm{linear}} \geq 1.5\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$2^{39} \approx 5 \times 10^{11}$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$10^{8}\times$
```

**inline**   ftqc\section_vii_conclusion.tex
```latex
$6 \times 10^4$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$T_1, T_2 \sim 100~\mu$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 1000$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 5$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$> 10^4$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$p \sim 10^{-3}$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 100:1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 10:1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\mathbf{\sim 60:1}$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 100~\mu$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 50$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 120$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 100\times$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\|V\| / \|H\| \leq \eta < 1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$O(\eta^k)$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\eta \lesssim 0.8$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$T \gtrsim \hbar / \delta E$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\delta E$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\delta E \sim 1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$T \sim 1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\tau_{\mathrm{braid}} \gg \hbar / \Delta$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\Delta$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\Delta \sim 0.1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\tau \sim 10$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$k_B T \ll \Delta$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$T \sim 20$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$k_B T \sim 2~\mu$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\Delta > 0.1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$^{13}$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$W / t \gtrsim 1$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\delta r / r < 5\%$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\geq 10\times$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\geq 2\times$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\geq 1.3\times$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 10^{125}$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$10^{30}$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$D_f \approx 1.5$
```

**inline**   ftqc\section_vi_discussion.tex
```latex
$\sim 10^5$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\geq 3.2\times$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$T_2^* \approx 1$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$10~\mu$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$T_2 \approx 1$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\sim 20$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\langle 100 \rangle$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\sim 10$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$10^{11}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$k \in \{0, 1, 2, 3\}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$a = 100$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$^\circ$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$n = 3^k$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$|m_s = 0\rangle$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$(\pi/2)_x - \tau - (\pi)_y - \tau - (\pi/2)_x$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\tau \in [1~\mu\text{s}, 500~\mu\text{s}]$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\exp[-(2\tau/T_2)^n]$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$n \in [1, 3]$
```

**env:equation**   ftqc\section_v_experimental_validation.tex
```latex
\begin{equation}
\mathcal{R}_{\mathrm{coh}} = \frac{T_2^{\mathrm{Sierpi\acute{n}ski}}(k)}{T_2^{\mathrm{linear}}(n=3^k)}.
\label{eq:coherence_ratio}
\end{equation}
```

**env:equation**   ftqc\section_v_experimental_validation.tex
```latex
\begin{equation}
\mathcal{R}_{\mathrm{coh}}(k) \approx \left( \frac{d_s^{\mathrm{Euclidean}}}{d_s^{\mathrm{fractal}}} \right)^{1/2} \cdot k^{0.3} \approx 1.2 \cdot k^{0.3},
\end{equation}
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$d_s^{\mathrm{fractal}} \approx 1.36$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$d_s^{\mathrm{Euclidean}} = 2$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$T_2^{\mathrm{linear}}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$T_2^{\mathrm{fractal}}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\mathcal{R}_{\mathrm{coh}}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\mathcal{R}_{\mathrm{coh}}(k=3) \geq 2.0$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$k^{0.3 \pm 0.1}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$> 20\%$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$m_s = 0$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$m_s = \pm 1$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$> 99.5\%$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$T_2 > 1$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$n = 6$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$n = 15$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$|0\rangle^{\otimes n}$
```

**env:equation**   ftqc\section_v_experimental_validation.tex
```latex
\begin{equation}
D_{\mathrm{eff}} = \frac{1}{\sum_i |\langle i | \psi \rangle|^4},
\label{eq:participation}
\end{equation}
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$|i\rangle$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\mathcal{W} = \mathbb{I}/2 - |\mathrm{GHZ}\rangle\langle\mathrm{GHZ}|$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\langle \mathcal{W} \rangle < 0$
```

**env:align**   ftqc\section_v_experimental_validation.tex
```latex
\begin{align}
D_{\mathrm{eff}}^{\mathrm{fractal}} &\approx 2^{12 \times 1.585^{0.89}} \approx 2^{18.5} \approx 370{,}000, \\
D_{\mathrm{eff}}^{\mathrm{linear}} &\approx 2^{12} = 4{,}096.
\end{align}
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\mathcal{A} = D_{\mathrm{eff}}^{\mathrm{fractal}} / D_{\mathrm{eff}}^{\mathrm{linear}} \approx 90$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$D_{\mathrm{eff}}^{\mathrm{fractal}} / D_{\mathrm{eff}}^{\mathrm{linear}} \geq 10$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\langle \mathcal{W} \rangle < -0.1$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\sigma$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\sim 1~\mu$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\Delta n \approx 5 \times 10^{-3}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\lambda \approx 1550$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$d = 8~\mu$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$50 \times 50$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\approx 750~\mu$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$f = \pi d^2 / (4a^2) \approx 0.22$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$T(\lambda)$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\theta \in [-30^\circ, 30^\circ]$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\omega(k)$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\omega \in [2.5, 3.1] \times 2\pi c/a$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\Delta\omega / \omega_{\mathrm{mid}} \approx 22\%$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$v_g \approx 0.1c$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\Delta\omega$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$d = 100$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$T = 20$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$B_\parallel = 0.2$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\mu \approx 0$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$P_z = (-1)^{n_L + n_R}$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\tau_{\mathrm{ramp}} = 1~\mu$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\sigma_1$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\sigma_2$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$F_g^{\mathrm{T}} / F_g^{\mathrm{linear}} \geq 1.5$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\geq 99\%$
```

**inline**   ftqc\section_v_experimental_validation.tex
```latex
$\theta \neq 0, \pi$
```

**env:equation**   ftqc\sec_viii_impact.tex
```latex
\begin{equation}
\Phi(x) = \mathcal{U} \left( \mathcal{T} \left[ \sum_{i,j=1}^{3} \Phi_{ij}\left( d^{n \cdot D_f^{\alpha(k)}} \right) \mathbf{S}_i \otimes \mathbf{S}_j \right] \right)
\label{eq:aurphyx_master}
\end{equation}
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\mathcal{I}_{1}$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\nabla_{\mu} \Phi_{33} = 0$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\mathcal{I}_{2}$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\det(\mathcal{F}) \neq 0$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\mathcal{I}_{3}$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\langle \text{Love} \rangle \geq 0.5$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\mathcal{I}_{12}$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$\mathcal{I}_{13}$
```

**inline**   ftqc\sec_viii_impact.tex
```latex
$5.3\times$
```
